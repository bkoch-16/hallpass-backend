$kind: http-request
name: Get District
url: "{{Base}}/api/districts/:id"
method: GET
description: Get a single district by ID. Requires SUPER_ADMIN role.
pathVariables:
  - key: id
    value: 1
order: 3000
