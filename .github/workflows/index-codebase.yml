name: Index Codebase

on:
  push:
    branches: [develop, main]

jobs:
  index:
    name: Generate context document
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10.30.1

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run indexer
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: pnpm tsx scripts/index-codebase.ts

      - name: Commit context file
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add "${GITHUB_REF_NAME}-context.md"
          git diff --cached --quiet || git commit -m "chore: update ${GITHUB_REF_NAME}-context.md [skip ci]"
          git push
