$kind: http-request
name: Batch Get User
url: "{{Base}}/api/users?ids=id1,id2"
method: GET
description: Fetch up to 100 users by a comma-separated list of IDs. Requires TEACHER role or higher.
queryParams:
  - key: ids
    value: id1,id2
order: 1000
